import api from "../../api/instance.js";

export async function onFollowProfile(event) {
    event.preventDefault()

    try {
        // const profile = await api.profiles.follow()
    } catch (error) {
        alert("Could not ")
    }
}